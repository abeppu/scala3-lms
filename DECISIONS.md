# Design Decisions

The canonical design-decision log lives in `doc/DECISIONS.md`.

This top-level pointer is kept so older references to `DECISIONS.md` still land
on the maintained document instead of a stale duplicate.
